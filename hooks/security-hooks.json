{
  "PreToolUse": [
    {
      "matcher": "Edit|MultiEdit|Write",
      "hooks": [
        {
          "type": "command",
          "command": "bwc add --hook file-protection"
        }
      ],
      "_comment": "file-protection: Protect critical files from accidental modification"
    },
    {
      "matcher": "Edit|MultiEdit|Write",
      "hooks": [
        {
          "type": "command",
          "command": "bwc add --hook file-protection-hook"
        }
      ],
      "_comment": "file-protection-hook: Protect critical files from accidental modification"
    }
  ],
  "PostToolUse": [
    {
      "matcher": "Edit|Write",
      "hooks": [
        {
          "type": "command",
          "command": "bwc add --hook security-scanner"
        }
      ],
      "_comment": "security-scanner: Scan code for security vulnerabilities and secrets after modifications"
    }
  ]
}